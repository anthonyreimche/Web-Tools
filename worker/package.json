{
  "name": "safelight-web-tools-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "esbuild src/index.js --bundle --format=esm --outfile=dist/worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "wrangler": "^3.78.0"
  }
}
